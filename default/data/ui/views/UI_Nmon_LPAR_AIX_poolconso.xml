<form stylesheet="singlevalue.css,ui_simple.css">
  <label>NMON - LPAR, Pool Virtual CPU Usage (AIX)</label>

  <fieldset autoRun="false" submitButton="false">

    <input type="time" token="timerange" searchWhenChanged="true">
      <label>Time Range:</label>
      <default>
        <earliestTime>-24h</earliestTime>
        <latestTime>now</latestTime>
      </default>

    </input>
      <input type="dropdown" token="timefilter" searchWhenChanged="true">
        <label>Time Filtering:</label>
        <choice value="No_Filter">No Filter (24/24, 7/7)</choice>
        <choice value="Day_BusinessDays_8h-19h">Day Business (08h-19h)</choice>
        <choice value="Day_WeekEnd_8h-19h">Day WE (08h-19h)</choice>
        <choice value="Day_AllDays_8h-19h">Day Week (08h-19h)</choice>
        <choice value="Night_BusinessDays_19h-8h">Night Business (19h-08h)</choice>
        <choice value="Night_WeekEnd_19h-8h">Night WE (19h-08h)</choice>
        <choice value="Night_AllDays_19h-8h">Night All Days (19h-08h)</choice>
        <default>No_Filter</default>
      </input>

    <input type="radio" token="appfilter" searchWhenChanged="true">
      <label>Filter Invalid APP:</label>
      <default>CPU.PoolIdle!=0</default>
      <choice value="CPU.PoolIdle!=0">True</choice>
      <choice value="CPU.PoolIdle=*">False</choice> 
   </input>

    <input type="multiselect" token="Pool_id" searchWhenChanged="true">
      <label>Pool IDs: (0 for DefaultPool)</label>
      <!-- Populating Data Model Search -->
      <search id="PoolidSearch">
        <query>| tstats count AS count from datamodel=NMON_Data_CPU where (nodename = CPU.LPAR) groupby "CPU.LPAR.Pool_id" prestats=true
| stats dedup_splitvals=t count AS count by "CPU.LPAR.Pool_id"
| sort limit=0 "CPU.LPAR.Pool_id"
| fields - _span | fillnull count | fields "CPU.LPAR.Pool_id"</query>
        <earliest>$timerange.earliest$</earliest>
        <latest>$timerange.latest$</latest>
      </search>
      <valuePrefix>CPU.LPAR.Pool_id="</valuePrefix>
      <valueSuffix>"</valueSuffix>
      <delimiter> OR </delimiter>
      <choice value="*">Any</choice>
      <default>*</default>
      <fieldForLabel>CPU.LPAR.Pool_id</fieldForLabel>
      <fieldForValue>CPU.LPAR.Pool_id</fieldForValue>
    </input>

    <input type="multiselect" token="frameID" searchWhenChanged="true">
      <label>Frame IDs:</label>
      <!-- Populating Data Model Search -->
      <search id="frameIDSearch">
        <query>| tstats count AS Count from datamodel=NMON_Data_CPU where (nodename = CPU.LPAR) ($Pool_id$) ($appfilter$) groupby "CPU.frameID" prestats=true
| stats dedup_splitvals=t count AS Count by "CPU.frameID"
| sort limit=0 "CPU.frameID"
| fields - _span | fillnull Count | fields "CPU.frameID"</query>
        <earliest>$timerange.earliest$</earliest>
        <latest>$timerange.latest$</latest>
      </search>
      <valuePrefix>CPU.frameID="</valuePrefix>
      <valueSuffix>"</valueSuffix>
      <delimiter> OR </delimiter>
      <choice value="*">Any</choice>
      <default>*</default>
      <fieldForLabel>CPU.frameID</fieldForLabel>
      <fieldForValue>CPU.frameID</fieldForValue>
    </input>

    <input type="text" token="hostname-prefilter" searchWhenChanged="true">
      <label>Optional: Filter hosts populating</label>
      <default>*</default>
    </input>    

    <input type="multiselect" token="hostname" searchWhenChanged="true">
      <label>Hosts Selection:</label>
      <!-- Populating Data Model Search -->
      <search id="baseSearch">
        <query>| tstats count AS Count from datamodel=NMON_Data_CPU where (nodename = CPU.LPAR) ($Pool_id$) ($appfilter$) (CPU.hostname=$hostname-prefilter$) groupby "CPU.hostname" prestats=true
| stats dedup_splitvals=t count AS Count by "CPU.hostname"
| sort limit=0 "CPU.hostname" | fields - _span
| fillnull Count | fields CPU.hostname, Count</query>
        <earliest>$timerange.earliest$</earliest>
        <latest>$timerange.latest$</latest>
      </search>
      <valuePrefix>CPU.hostname=</valuePrefix>
      <delimiter> OR </delimiter>
      <choice value="*">ALL Hosts</choice>
      <fieldForLabel>CPU.hostname</fieldForLabel>
      <fieldForValue>CPU.hostname</fieldForValue>
    </input>

    <input type="dropdown" token="aggregate" searchWhenChanged="true">
      <label>Aggregate:</label>
      <choice value="fields *">Single Series</choice>
      <choice value="stats avg(CPU.poolCPUs) AS CPU.poolCPUs, avg(CPU.LPAR.Pool_usage) AS CPU.LPAR.Pool_usage by _time | eval CPU.frameID=&quot;aggreg_frameID&quot; | eval CPU.hostname=&quot;aggreg_hostname&quot;">Average by Time interval</choice>
      <choice value="stats avg(CPU.poolCPUs) AS CPU.poolCPUs, avg(CPU.LPAR.Pool_usage) AS CPU.LPAR.Pool_usage by _time,CPU.frameID | eval CPU.hostname='CPU.frameID'">Average by Time interval,frameID</choice>
      <choice value="stats max(CPU.poolCPUs) AS CPU.poolCPUs, max(CPU.LPAR.Pool_usage) AS CPU.LPAR.Pool_usage by _time | eval CPU.frameID=&quot;aggreg_frameID&quot; | eval CPU.hostname=&quot;aggreg_hostname&quot;">Max by Time interval</choice>
      <choice value="stats max(CPU.poolCPUs) AS CPU.poolCPUs, max(CPU.LPAR.Pool_usage) AS CPU.LPAR.Pool_usage by _time,CPU.frameID | eval CPU.hostname='CPU.frameID'">Max by Time interval,frameID</choice>
      <choice value="stats sum(CPU.poolCPUs) AS CPU.poolCPUs, sum(CPU.LPAR.Pool_usage) AS CPU.LPAR.Pool_usage by _time | eval CPU.frameID=&quot;aggreg_frameID&quot; | eval CPU.hostname=&quot;aggreg_hostname&quot;">Sum by Time interval</choice>      
      <choice value="stats sum(CPU.poolCPUs) AS CPU.poolCPUs, sum(CPU.LPAR.Pool_usage) AS CPU.LPAR.Pool_usage by _time,CPU.frameID | eval CPU.hostname='CPU.frameID'">Sum by Time interval,frameID</choice>
      <default>fields *</default>
    </input>

    <input type="dropdown" token="statsmode" searchWhenChanged="true">
      <label>Stats mode:</label>
      <default>max</default>
      <choice value="max">Max</choice>
      <choice value="avg">Avg</choice>
      <choice value="min">Min</choice>
      <choice value="median">Median</choice>
      <choice value="mode">Mode</choice>
      <choice value="range">Range</choice>
    </input>

    <input type="dropdown" token="indicator" searchWhenChanged="true">
      <label>Show CPU load by:</label>
      <default>rename CPU.LPAR.Pool_usage AS usage</default>
      <choice value="rename CPU.LPAR.Pool_usage AS usage">VP Usage</choice>
      <choice value="rename CPU.LPAR.lpar_vp_PCT_usage AS usage">Usage vs VirtualCPUs % Usage</choice>
      <choice value="rename CPU.LPAR.lpar_ec_usage AS usage">Usage vs Entitled</choice>
      <choice value="rename CPU.LPAR.lpar_ec_Pct_usage AS usage">Usage vs Entitled % usage</choice>
    </input>

    <input type="checkbox" token="charting" searchWhenChanged="true">
      <label>Charting:</label>
      <default>fields *</default>
      <choice value="fields *">On</choice>
    </input>
    
  </fieldset>

  <!-- Global Search -->
  
        <search id="Global">
          <query>| tstats max("CPU.poolCPUs") AS "CPU.poolCPUs" max("CPU.LPAR.Pool_usage") AS "CPU.LPAR.Pool_usage" values("CPU.LPAR.Pool_id") AS "CPU.LPAR.Pool_id" from datamodel=NMON_Data_CPU where (nodename = CPU.LPAR) 
(CPU.hostname=$hostname-prefilter$) ($frameID$) ($hostname$) ($Pool_id$) ($appfilter$) `$timefilter$(CPU)` groupby _time, "CPU.frameID", "CPU.hostname" prestats=true `inline_customspan(type=LPAR,earliest=$timerange.earliest$,latest=$timerange.latest$)`
| stats dedup_splitvals=t max("CPU.poolCPUs") AS "CPU.poolCPUs" max("CPU.LPAR.Pool_usage") AS "CPU.LPAR.Pool_usage" values("CPU.LPAR.Pool_id") AS "CPU.LPAR.Pool_id" by _time, "CPU.frameID", "CPU.hostname"
| $aggregate$ | eval CPU.LPAR.Pool_id=if(isnull(CPU.LPAR.Pool_id), "N/A", CPU.LPAR.Pool_id)
| sort limit=0 _time
| rename CPU.LPAR.Pool_usage AS usage
| stats min(usage) As min_usage, avg(usage) As avg_usage, max(usage) As max_usage, values("CPU.LPAR.Pool_id") AS "CPU.LPAR.Pool_id", max("CPU.poolCPUs") AS "CPU.poolCPUs", sparkline($statsmode$(usage)) As sparkline by "CPU.frameID", "CPU.hostname"
| rename "CPU.frameID" AS frameID, "CPU.hostname" AS hostname, "CPU.LPAR.Pool_id" AS "Pool ID", "CPU.poolCPUs" AS "CPUs in Pool" | sort limit=0 frameID | eval avg_usage=round(avg_usage,2)</query>
          <earliest>$timerange.earliest$</earliest>
          <latest>$timerange.latest$</latest>
        </search>

  <row>
    <panel>
    
      <single>
        <search base="Global">
          <query>stats max(max_usage) AS max</query>
        </search>
        <option name="underLabel">Maximum Load</option>
        <option name="linkView">search</option>
      </single>

      <single>
        <search base="Global">
          <query>stats avg(avg_usage) AS avg | eval avg=round(avg,2)</query>
        </search>
        <option name="underLabel">Average Load</option>
        <option name="linkView">search</option>
      </single>

      <single>
        <search base="Global">
          <query>stats min(min_usage) AS min</query>
        </search>
        <option name="underLabel">Minimal Load</option>
        <option name="linkView">search</option>
      </single>    
    
      <table>
        <search base="Global">
          <query>fields hostname,frameID,"Pool ID","CPUs in Pool",max_usage,avg_usage,min_usage,sparkline</query>
        </search>
        <option name="wrap">true</option>
        <option name="rowNumbers">false</option>
        <option name="dataOverlayMode">none</option>
        <option name="drilldown">cell</option>
        <option name="count">10</option>
        <format type="sparkline" field="sparkline">
          <option name="lineColor">#5379af</option>
          <option name="fillColor">#CCDDFF</option>
          <option name="lineWidth">1</option>
          <option name="height">25px</option>
        </format>
      </table>

    </panel>
  </row>
  
  <row>
    <panel>
      <title>Charting parameters:</title>
      <input type="dropdown" token="addindicator" searchWhenChanged="true">
        <label>Pool additional Series:</label>
        <default>none</default>
        <choice value="">none</choice>
        <choice value="max(CPU.poolCPUs) AS poolCPUs">poolCPUs: Nbr of CPUs in Pool</choice>
      </input>      
      <input type="dropdown" token="chart" searchWhenChanged="true">
        <label>Select a type of chart:</label>
        <default>line</default>
        <choice value="area">Area</choice>
        <choice value="line">Line</choice>
        <choice value="column">Column</choice>
        <choice value="bar">Bar</choice>
      </input>
      <input type="dropdown" token="charting.chart.nullValueMode" searchWhenChanged="true">
        <label>Missing Data:</label>
        <default>gaps</default>
        <choice value="gaps">Gaps</choice>
        <choice value="connect">Connect</choice>
        <choice value="zero">Zero</choice>
      </input>
      <input type="dropdown" token="chart.stackingmode" searchWhenChanged="true">
        <label>Select a stacking mode:</label>
        <default>unstacked</default>
        <choice value="stacked">stacked (lines excluded)</choice>
        <choice value="stacked100">100% stacked (lines excluded)</choice>
        <choice value="unstacked">unstacked</choice>
      </input>
      <input type="dropdown" token="charting.legend.placement" searchWhenChanged="true">
        <label>Legend placement:</label>
        <default>bottom</default>
        <choice value="bottom">Bottom</choice>
        <choice value="top">Top</choice>
        <choice value="left">left</choice>
        <choice value="right">right</choice>
        <choice value="center">center</choice>
        <choice value="none">none</choice>
      </input>
    </panel>
  </row>
  
  <row>
    <panel>
      <chart>
  			<search id="timechart">
    		  <query>| tstats max("CPU.poolCPUs") AS "CPU.poolCPUs" max("CPU.LPAR.Pool_usage") AS "CPU.LPAR.Pool_usage" values("CPU.LPAR.Pool_id") AS "CPU.LPAR.Pool_id" from datamodel=NMON_Data_CPU where (nodename = CPU.LPAR) 
(CPU.hostname=$hostname-prefilter$) ($frameID$) ($hostname$) ($Pool_id$) ($appfilter$) `$timefilter$(CPU)` groupby _time, "CPU.frameID", "CPU.hostname" prestats=true `inline_customspan(type=LPAR,earliest=$timerange.earliest$,latest=$timerange.latest$)`
| stats dedup_splitvals=t max("CPU.poolCPUs") AS "CPU.poolCPUs" max("CPU.LPAR.Pool_usage") AS "CPU.LPAR.Pool_usage" values("CPU.LPAR.Pool_id") AS "CPU.LPAR.Pool_id" by _time, "CPU.frameID", "CPU.hostname"
| sort limit=0 _time
| $aggregate$
| rename CPU.LPAR.Pool_usage AS usage
| $charting$
| timechart `inline_customspan(type=LPAR,earliest=$timerange.earliest$,latest=$timerange.latest$)` useother=f limit=0 
$statsmode$(usage) As usage $addindicator$ by "CPU.hostname"</query>
    		  <earliest>$timerange.earliest$</earliest>
    		  <latest>$timerange.latest$</latest>  
  		  </search>  
        <option name="charting.axisTitleX.visibility">visible</option>
        <option name="charting.axisTitleY.visibility">visible</option>
        <option name="charting.axisX.scale">linear</option>
        <option name="charting.axisY.scale">linear</option>
        <option name="charting.chart">$chart$</option>
        <option name="charting.chart.nullValueMode">$charting.chart.nullValueMode$</option>
        <option name="charting.chart.sliceCollapsingThreshold">0.01</option>
        <option name="charting.chart.stackMode">$chart.stackingmode$</option>
        <option name="charting.chart.style">shiny</option>
        <option name="charting.drilldown">all</option>
        <option name="charting.layout.splitSeries">0</option>
        <option name="charting.legend.labelStyle.overflowMode">ellipsisMiddle</option>
        <option name="charting.legend.placement">$charting.legend.placement$</option>
        <option name="height">680px</option>
        <option name="charting.axisTitleX.text">Time</option>
        <option name="charting.axisTitleY.text">Virtual CPUs</option>
        <option name="charting.axisY.minimumNumber">0</option>
        <option name="charting.axisLabelsX.majorLabelStyle.overflowMode">ellipsisNone</option>
        <option name="charting.axisLabelsX.majorLabelStyle.rotation">0</option>
        <option name="charting.axisTitleY2.visibility">visible</option>
        <option name="charting.axisY2.enabled">false</option>
        <option name="charting.axisY2.scale">inherit</option>
      </chart>
    </panel>
  </row>
  
</form>