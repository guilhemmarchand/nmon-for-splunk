<form stylesheet="singlevalue.css,ui_simple.css" script="link_switcher.js, autodiscover.js">
  <label>NMON - IOADAPT, Adapters Read/Write / Total Operations</label>
  <description/>
  <fieldset autoRun="false" submitButton="false">

    <input type="time" token="timerange" searchWhenChanged="true">
      <label>Time Range:</label>
      <default>
        <earliestTime>-24h</earliestTime>
        <latestTime>now</latestTime>
      </default>
    </input>

    <input type="dropdown" token="timefilter" searchWhenChanged="true">
      <label>Time Filtering:</label>
      <choice value="No_Filter">No Filter (24/24, 7/7)</choice>
      <choice value="Day_BusinessDays_8h-19h">Day Business (08h-19h)</choice>
      <choice value="Day_WeekEnd_8h-19h">Day WE (08h-19h)</choice>
      <choice value="Day_AllDays_8h-19h">Day Week (08h-19h)</choice>
      <choice value="Night_BusinessDays_19h-8h">Night Business (19h-08h)</choice>
      <choice value="Night_WeekEnd_19h-8h">Night WE (19h-08h)</choice>
      <choice value="Night_AllDays_19h-8h">Night All Days (19h-08h)</choice>
      <default>No_Filter</default>
    </input>

    <input type="multiselect" token="frameID" searchWhenChanged="true">
      <label>Frame IDs:</label>
      <!-- Populating Data Model Search -->
      <search id="frameIDSearch">
        <query>| tstats count AS Count from datamodel=NMON_Data_IOADAPT where (nodename = IOADAPT) groupby "IOADAPT.frameID" prestats=true
| stats dedup_splitvals=t count AS Count by "IOADAPT.frameID"
| sort limit=0 "IOADAPT.frameID"
| fields - _span | fillnull Count | fields "IOADAPT.frameID"</query>
        <earliest>$timerange.earliest$</earliest>
        <latest>$timerange.latest$</latest>
      </search>
      <valuePrefix>IOADAPT.frameID="</valuePrefix>
      <valueSuffix>"</valueSuffix>
      <delimiter> OR </delimiter>
      <choice value="*">Any</choice>
      <default>*</default>
      <fieldForLabel>IOADAPT.frameID</fieldForLabel>
      <fieldForValue>IOADAPT.frameID</fieldForValue>
    </input>

    <input type="text" token="hostname-prefilter" searchWhenChanged="true">
      <label>Optional: Filter hosts populating</label>
      <default>*</default>
    </input>

    <input type="multiselect" token="hostname" searchWhenChanged="true">
      <label>Hosts Selection:</label>
      <!-- Populating Data Model Search -->
      <search id="baseSearch">
        <query>| tstats count AS Count from datamodel=NMON_Data_IOADAPT where (nodename = IOADAPT) ($frameID$) (IOADAPT.hostname="$hostname-prefilter$") groupby "IOADAPT.hostname" prestats=true
| stats dedup_splitvals=t count AS Count by "IOADAPT.hostname"
| sort limit=0 "IOADAPT.hostname"
| rename "IOADAPT.hostname" AS hostname | fillnull Count | fields hostname, Count</query>
        <earliest>$timerange.earliest$</earliest>
        <latest>$timerange.latest$</latest>
      </search>
      <valuePrefix>IOADAPT.hostname="</valuePrefix>
      <valueSuffix>"</valueSuffix>
      <delimiter> OR </delimiter>
      <choice value="*">ALL Hosts</choice>
      <fieldForLabel>hostname</fieldForLabel>
      <fieldForValue>hostname</fieldForValue>
    </input>

    <input type="multiselect" token="device" searchWhenChanged="true">
      <label>Interfaces Selection:</label>
      <!-- Populating Data Model Search -->
      <search id="baseSearch2">
        <query>| tstats count AS Count from datamodel=NMON_Data_IOADAPT where (nodename = IOADAPT) ($frameID$) (IOADAPT.hostname="$hostname-prefilter$") ($hostname$) groupby "IOADAPT.hostname", "IOADAPT.device" prestats=true
| stats dedup_splitvals=t count AS Count by "IOADAPT.device" 
| sort limit=0 "IOADAPT.device" 
| rename "IOADAPT.device" AS device | fields device | dedup device | sort device</query>
        <earliest>$timerange.earliest$</earliest>
        <latest>$timerange.latest$</latest>
      </search>
      <choice value="*">ALL INTERFACES</choice>
      <valuePrefix>IOADAPT.device="</valuePrefix>
      <valueSuffix>"</valueSuffix>
      <delimiter> OR </delimiter>
      <choice value="*">ALL devices</choice>
      <fieldForLabel>device</fieldForLabel>
      <fieldForValue>device</fieldForValue>
    </input>

    <input type="dropdown" token="aggregate" searchWhenChanged="true">
      <label>Aggregate:</label>
      <choice value="fields *">Single Series</choice>
      <choice value="stats avg(IOADAPT.value) As IOADAPT.value by _time,IOADAPT.hostname | eval IOADAPT.device=&quot;aggregated_device&quot;">Average by Time interval, host</choice>
      <choice value="stats avg(IOADAPT.value) As IOADAPT.value by _time | eval IOADAPT.hostname=aggregated_host | eval IOADAPT.device=&quot;aggregated_device&quot;">Average by Time interval</choice>      
      <choice value="stats max(IOADAPT.value) As IOADAPT.value by _time,IOADAPT.hostname | eval IOADAPT.device=&quot;aggregated_device&quot;">Max by Time interval, host</choice>
      <choice value="stats max(IOADAPT.value) As IOADAPT.value by _time | eval IOADAPT.hostname=aggregated_host | eval IOADAPT.device=&quot;aggregated_device&quot;">Max by Time interval</choice>
      <choice value="stats sum(IOADAPT.value) As IOADAPT.value by _time,IOADAPT.hostname | eval IOADAPT.device=&quot;aggregated_device&quot;">Sum by Time interval, host</choice>
      <choice value="stats sum(IOADAPT.value) As IOADAPT.value by _time | eval IOADAPT.hostname=aggregated_host | eval IOADAPT.device=&quot;aggregated_device&quot;">Sum by Time interval</choice>
      <default>fields *</default>
    </input>    
    <input type="dropdown" token="statsmode" searchWhenChanged="true">
      <label>Select a stats mode:</label>
      <default>max</default>
      <choice value="max">Max</choice>
      <choice value="avg">Avg</choice>
      <choice value="min">Min</choice>
      <choice value="median">Median</choice>
      <choice value="mode">Mode</choice>
      <choice value="range">Range</choice>
    </input>
  </fieldset>
  
<!-- Base Searches for PostProcessing -->
  
  <search id="tablestats">
    <query>| tstats max(IOADAPT.value) As IOADAPT.value from datamodel=NMON_Data_IOADAPT
where (nodename = IOADAPT) (IOADAPT.hostname=$hostname-prefilter$) ($frameID$) ($hostname$) ($device$) `$timefilter$(IOADAPT)`
groupby _time, "IOADAPT.hostname", "IOADAPT.device" prestats=true
| stats dedup_splitvals=t max(IOADAPT.value) As IOADAPT.value by _time, "IOADAPT.hostname", "IOADAPT.device"
| $aggregate$
| stats max(IOADAPT.value) As max_value, avg(IOADAPT.value) As avg_value, min(IOADAPT.value) As min_value, sparkline(max(IOADAPT.value)) As sparkline by "IOADAPT.hostname", "IOADAPT.device"
| sort limit=0 "IOADAPT.hostname" | rename "IOADAPT.hostname" AS hostname, "IOADAPT.device" AS device</query>
    <earliest>$timerange.earliest$</earliest>
    <latest>$timerange.latest$</latest>  
  </search>
    
  <row>
    <panel>
      <table>
        <title>Read/Write Operations Table Stats</title>
		  <search base="tablestats">
		    <query>| where ( device LIKE "%KB/s%")</query>
		  </search>
        <option name="wrap">true</option>
        <option name="rowNumbers">false</option>
        <option name="dataOverlayMode">none</option>
        <option name="drilldown">cell</option>
        <option name="count">10</option>
        <!-- Set sparkline options here; make sure that field matches field name of the search results -->
        <format type="sparkline" field="sparkline">
          <option name="lineColor">#5379af</option>
          <option name="fillColor">#CCDDFF</option>
          <option name="lineWidth">1</option>
          <option name="height">25px</option>
        </format>
      </table>
    </panel>
    <panel>
      <table>
        <title>Total I/O Operations Table Stats (tps)</title>
		  <search base="tablestats">
		    <query>| where ( device LIKE "%tps%")</query>
		  </search>
        <option name="wrap">true</option>
        <option name="rowNumbers">false</option>
        <option name="dataOverlayMode">none</option>
        <option name="drilldown">cell</option>
        <option name="count">10</option>
        <!-- Set sparkline options here; make sure that field matches field name of the search results -->
        <format type="sparkline" field="sparkline">
          <option name="lineColor">#5379af</option>
          <option name="fillColor">#CCDDFF</option>
          <option name="lineWidth">1</option>
          <option name="height">25px</option>
        </format>
      </table>
    </panel>
  </row>  

  <row>
    <panel>
      <title>Charting parameters:</title>
      <input type="dropdown" token="chart" searchWhenChanged="true">
        <label>Select a type of chart:</label>
        <default>line</default>
        <choice value="area">Area</choice>
        <choice value="line">Line</choice>
        <choice value="column">Column</choice>
        <choice value="bar">Bar</choice>
      </input>
      <input type="dropdown" token="charting.chart.nullValueMode" searchWhenChanged="true">
        <label>Missing Data:</label>
        <default>gaps</default>
        <choice value="gaps">Gaps</choice>
        <choice value="connect">Connect</choice>
        <choice value="zero">Zero</choice>
      </input>
      <input type="dropdown" token="chart.stackingmode" searchWhenChanged="true">
        <label>Select a stacking mode:</label>
        <default>unstacked</default>
        <choice value="stacked">stacked (lines excluded)</choice>
        <choice value="stacked100">100% stacked (lines excluded)</choice>
        <choice value="unstacked">unstacked</choice>
      </input>
      <input type="dropdown" token="charting.legend.placement" searchWhenChanged="true">
        <label>Legend placement:</label>
        <default>bottom</default>
        <choice value="bottom">Bottom</choice>
        <choice value="top">Top</choice>
        <choice value="left">left</choice>
        <choice value="right">right</choice>
        <choice value="center">center</choice>
        <choice value="none">none</choice>
      </input>
    </panel>
  </row>

  <row>
    <panel>
      <html>
      <!-- Placeholder/container for the link switcher to appear -->
      <div class="link-switcher" data-items="link10,link11">Select a view:<!--Links go here-->
      </div>
    </html>
      <chart id="link10">
        <title>Data Rate (Kbytes/sec)</title>
		  <search>
		    <query>| tstats max(IOADAPT.value) As IOADAPT.value from datamodel=NMON_Data_IOADAPT
where (nodename = IOADAPT) (IOADAPT.hostname=$hostname-prefilter$) ($frameID$) ($hostname$) ($device$) (IOADAPT.device!="*tps*") `$timefilter$(IOADAPT)`
groupby _time, "IOADAPT.hostname", "IOADAPT.device" prestats=true `inline_customspan(type=IOADAPT,earliest=$timerange.earliest$,latest=$timerange.latest$)`
| stats dedup_splitvals=t max(IOADAPT.value) As IOADAPT.value by _time, "IOADAPT.hostname", "IOADAPT.device"
| $aggregate$
| bucket _time `inline_customspan(type=IOADAPT,earliest=$timerange.earliest$,latest=$timerange.latest$)`
| stats $statsmode$(IOADAPT.value) As value by _time,IOADAPT.hostname,IOADAPT.device
| rename IOADAPT.hostname AS hostname, IOADAPT.device AS device
| eval s1="value"
| makemv s1 
| mvexpand s1
| eval yval=case(s1=="value",value)
| eval series=hostname+":"+device+":"+s1
| xyseries _time,series,yval
| makecontinuous _time `inline_customspan(type=IOADAPT,earliest=$timerange.earliest$,latest=$timerange.latest$)`</query>
          <earliest>$timerange.earliest$</earliest>
          <latest>$timerange.latest$</latest>
        </search>
        <option name="charting.axisTitleX.visibility">visible</option>
        <option name="charting.axisTitleY.visibility">visible</option>
        <option name="charting.axisX.scale">linear</option>
        <option name="charting.axisY.scale">linear</option>
        <option name="charting.chart">$chart$</option>
        <option name="charting.chart.nullValueMode">$charting.chart.nullValueMode$</option>
        <option name="charting.chart.sliceCollapsingThreshold">0.01</option>
        <option name="charting.chart.stackMode">$chart.stackingmode$</option>
        <option name="charting.chart.style">shiny</option>
        <option name="charting.drilldown">all</option>
        <option name="charting.layout.splitSeries">0</option>
        <option name="charting.legend.labelStyle.overflowMode">ellipsisMiddle</option>
        <option name="charting.legend.placement">$charting.legend.placement$</option>
        <option name="height">680px</option>
        <option name="charting.axisTitleX.text">Time</option>
        <option name="charting.axisY.minimumNumber">0</option>
        <option name="charting.axisTitleY.text">Kbytes/sec</option>
        <option name="charting.axisLabelsX.majorLabelStyle.overflowMode">ellipsisNone</option>
        <option name="charting.axisLabelsX.majorLabelStyle.rotation">0</option>
        <option name="charting.axisTitleY2.visibility">visible</option>
        <option name="charting.axisY2.enabled">false</option>
        <option name="charting.axisY2.scale">inherit</option>
      </chart>
      <chart id="link11">
        <title>Total I/O Operations (tps)</title>
		  <search>
		    <query>| tstats max(IOADAPT.value) As IOADAPT.value from datamodel=NMON_Data_IOADAPT
where (nodename = IOADAPT) (IOADAPT.hostname=$hostname-prefilter$) ($frameID$) ($hostname$) ($device$) (IOADAPT.device="*tps*") `$timefilter$(IOADAPT)`
groupby _time, "IOADAPT.hostname", "IOADAPT.device" prestats=true `inline_customspan(type=IOADAPT,earliest=$timerange.earliest$,latest=$timerange.latest$)`
| stats dedup_splitvals=t max(IOADAPT.value) As IOADAPT.value by _time, "IOADAPT.hostname", "IOADAPT.device"
| $aggregate$
| bucket _time `inline_customspan(type=IOADAPT,earliest=$timerange.earliest$,latest=$timerange.latest$)`
| stats $statsmode$(IOADAPT.value) As value by _time,IOADAPT.hostname,IOADAPT.device
| rename IOADAPT.hostname AS hostname, IOADAPT.device AS device
| eval s1="value"
| makemv s1 
| mvexpand s1
| eval yval=case(s1=="value",value)
| eval series=hostname+":"+device+":"+s1
| xyseries _time,series,yval
| makecontinuous _time `inline_customspan(type=IOADAPT,earliest=$timerange.earliest$,latest=$timerange.latest$)`</query>
          <earliest>$timerange.earliest$</earliest>
          <latest>$timerange.latest$</latest>
        </search>
        <option name="charting.axisTitleX.visibility">visible</option>
        <option name="charting.axisTitleY.visibility">visible</option>
        <option name="charting.axisX.scale">linear</option>
        <option name="charting.axisY.scale">linear</option>
        <option name="charting.chart">$chart$</option>
        <option name="charting.chart.nullValueMode">$charting.chart.nullValueMode$</option>
        <option name="charting.chart.sliceCollapsingThreshold">0.01</option>
        <option name="charting.chart.stackMode">$chart.stackingmode$</option>
        <option name="charting.chart.style">shiny</option>
        <option name="charting.drilldown">all</option>
        <option name="charting.layout.splitSeries">0</option>
        <option name="charting.legend.labelStyle.overflowMode">ellipsisMiddle</option>
        <option name="charting.legend.placement">$charting.legend.placement$</option>
        <option name="height">680px</option>
        <option name="charting.axisTitleX.text">Time</option>
        <option name="charting.axisY.minimumNumber">0</option>
        <option name="charting.axisTitleY.text">I/O per Second</option>
      </chart>
    </panel>
  </row>
</form>