<form stylesheet="singlevalue.css,ui_simple.css">
  <label>NMON - CONFIG, Linux Show Host Configuration</label>
  <fieldset autoRun="false" submitButton="false">
    <input type="time" token="timerange" searchWhenChanged="true">
      <label>TimeRange:</label>
      <default>
        <earliestTime>-24h</earliestTime>
        <latestTime>now</latestTime>
      </default>
    </input>
    <input type="radio" token="osfilter" searchWhenChanged="true">
      <label>Type of OS filter:</label>
      <default>| search [ `nmon_inventory` | search OStype=Linux | stats count by hostname | fields hostname ]</default>
      <choice value="">Any OS</choice>
      <choice value="| search [ `nmon_inventory` | search OStype=Linux | stats count by hostname | fields hostname ]">Linux</choice>
    </input>
    <input type="text" token="hostname-prefilter" searchWhenChanged="true">
      <label>Optional: Filter hosts populating</label>
      <prefix>hostname="</prefix>
      <suffix>"</suffix>
      <default>*</default>
    </input>    
    <input type="dropdown" token="hostname" searchWhenChanged="true">
      <label>Host Selection:</label>
      <prefix>hostname="</prefix>
      <suffix>"</suffix>
      <populatingSearch earliest="$timerange.earliest$" latest="$timerange.latest$" fieldForLabel="hostname" fieldForValue="hostname">
        <![CDATA[index=nmon sourcetype=nmon_config $hostname-prefilter$ | stats count by hostname | dedup hostname | sort hostname $osfilter$]]>
      </populatingSearch>
    </input>
  </fieldset>
  <row>
    <panel>
      <input type="text" token="customsearch">
        <label>Search for a pattern (Will be highlighted):</label>
        <default/>
        <seed/>
        <suffix>*</suffix>
      </input>
      <event>
        <title>Host Configuration Elements (AAA and BBB sections extracted from NMON) - Last event of selected Period</title>
        <searchString>index=nmon sourcetype=nmon_config $hostname$ | head 1 | search $customsearch$</searchString>
        <earliestTime>$timerange.earliest$</earliestTime>
        <latestTime>$timerange.latest$</latestTime>
        <option name="count">10</option>
        <option name="list.drilldown">full</option>
        <option name="list.wrap">1</option>
        <option name="maxLines">0</option>
        <option name="raw.drilldown">full</option>
        <option name="rowNumbers">0</option>
        <option name="table.drilldown">1</option>
        <option name="table.wrap">1</option>
        <option name="type">list</option>
        <fields>[]</fields>
      </event>
    </panel>
  </row>
</form>