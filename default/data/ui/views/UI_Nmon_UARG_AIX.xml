<form stylesheet="singlevalue.css,ui_simple.css">
  <label>NMON - UARG, Search Commands Arguments</label>
  <fieldset autoRun="false" submitButton="false">
    <input type="time" token="timerange" searchWhenChanged="true">
      <label>TimeRange:</label>
      <default>
        <earliestTime>-24h</earliestTime>
        <latestTime>now</latestTime>
      </default>
    </input>

    <input type="radio" token="osfilter" searchWhenChanged="true">
      <label>Filter OS Type:</label>
      <default>AIX</default>
      <choice value="*">Any OS</choice>
      <choice value="AIX">AIX</choice>
    </input>

    <input type="text" token="PID" searchWhenChanged="true">
      <label>Optional: Filter PIDs</label>
      <prefix>PID="</prefix>
      <suffix>"</suffix>
      <default>*</default>
    </input>
    
    <input type="text" token="COMM" searchWhenChanged="true">
      <label>Optional: Filter Command name</label>
      <prefix>COMM="</prefix>
      <suffix>"</suffix>
      <default>*</default>
    </input>

    <input type="text" token="USER" searchWhenChanged="true">
      <label>Optional: Filter user</label>
      <prefix>USER="</prefix>
      <suffix>"</suffix>
      <default>*</default>
    </input>

    <input type="text" token="GROUP" searchWhenChanged="true">
      <label>Optional: Filter group</label>
      <prefix>GROUP="</prefix>
      <suffix>"</suffix>
      <default>*</default>
    </input>

    <input type="text" token="FullCommand" searchWhenChanged="true">
      <label>Optional: Filter FullCommand</label>
      <prefix>FullCommand="</prefix>
      <suffix>"</suffix>
      <default>*</default>
    </input>

    <input type="text" token="hostname-prefilter" searchWhenChanged="true">
      <label>Optional: Filter hosts populating</label>
      <default>*</default>
    </input>

    <input type="multiselect" token="hostname" searchWhenChanged="true">
      <label>Hosts Selection:</label>
      <!-- Populating Data Model Search -->
      <search id="baseSearch">
        <query>| tstats count AS Count from datamodel=NMON_Data_UARG where (nodename = UARG) (UARG.hostname="$hostname-prefilter$") (UARG.OStype="$osfilter$") groupby "UARG.hostname" prestats=true | stats dedup_splitvals=t count AS Count by "UARG.hostname" | sort limit=0 "UARG.hostname" | fields - _span | rename "UARG.hostname" AS hostname | fillnull Count | fields hostname, Count</query>
        <earliest>$timerange.earliest$</earliest>
        <latest>$timerange.latest$</latest>
      </search>
      <valuePrefix>hostname="</valuePrefix>
      <valueSuffix>"</valueSuffix>
      <delimiter> OR </delimiter>
      <fieldForLabel>hostname</fieldForLabel>
      <fieldForValue>hostname</fieldForValue>
    </input>

  </fieldset>
  <row>
    <panel>
      <input type="text" token="customsearch">
        <label>Search for a pattern (Will be highlighted):</label>
        <default/>
        <seed/>
        <suffix>*</suffix>
      </input>
      <event>
        <title>Events:</title>
        <searchString>index=nmon sourcetype=nmon_data type=UARG $hostname$ $PID$ $COMM$ $USER$ $GROUP$ $FullCommand$ | search $customsearch$</searchString>
        <earliestTime>$timerange.earliest$</earliestTime>
        <latestTime>$timerange.latest$</latestTime>
        <option name="count">10</option>
        <option name="list.drilldown">full</option>
        <option name="list.wrap">1</option>
        <option name="maxLines">0</option>
        <option name="raw.drilldown">full</option>
        <option name="rowNumbers">0</option>
        <option name="table.drilldown">1</option>
        <option name="table.wrap">1</option>
        <option name="type">list</option>
        <fields>[]</fields>
      </event>
      <table>
        <title>Table:</title>
        <searchString>index=nmon sourcetype=nmon_data type=UARG $hostname$ $PID$ $COMM$ $USER$ $GROUP$ $FullCommand$ | table _time,hostname,PID,PPID,COMM,THCOUNT,USER,GROUP,FullCommand</searchString>
        <earliestTime>$timerange.earliest$</earliestTime>
        <latestTime>$timerange.latest$</latestTime>
        <option name="wrap">true</option>
        <option name="rowNumbers">false</option>
        <option name="dataOverlayMode">none</option>
        <option name="drilldown">cell</option>
        <option name="count">10</option>
      </table>
    </panel>
  </row>
</form>