<setup>

  <block title="Activate NMON Monitoring for local host" endpoint="data/inputs/script" entity="nmon_helper.sh">

  <text>If you want to collect local NMON data, activate this script input:</text>

  <text>Note: For upgrade from version previous to V1.1.7, please clean your $SPLUNK_HOME/local/inputs.conf and restart Splunk</text>

    <input field="enabled">
      <label>Enable</label>
      <type>bool</type>
    </input>

  </block>

  <block title="Activate Purging NMON Repository" endpoint="data/inputs/script" entity="purge_nmon_repository.sh">

  <text>The default NMON Repository should be purged periodically to avoid useless multiplication of files</text>
  <text>When activated, this purge script will remove any nmon file older than 1 day:</text>


    <input field="enabled">
      <label>Enable</label>
      <type>bool</type>
    </input>

  <text>Default execution interval to each hour:</text>

    <input field="interval">
      <label>Interval</label>
      <type>text</type>
    </input>
  </block>
  
</setup>