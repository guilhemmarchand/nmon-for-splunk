<setup>

  <block title="NMON Monitoring for LINUX Hosts" endpoint="data/inputs/script" entity="nmon_for_linux.sh">

  <text>If this host is a Linux System and you want to collect local NMON data, activate this script input:</text>

    <input field="enabled">
      <label>Enable</label>
      <type>bool</type>
    </input>

  </block>


  <block title="NMON Monitoring for SOLARIS Hosts" endpoint="data/inputs/script" entity="nmon_for_solaris.sh">

  <text>If this host is a Solaris System and you want to collect local NMON data, activate this script input:</text>

    <input field="enabled">
      <label>Enable</label>
      <type>bool</type>
    </input>

  </block>

  <block title="Purging NMON Repository" endpoint="data/inputs/script" entity="purge_nmon_repository.sh">

  <text>The default NMON Repository should be purged periodically to avoid useless multiplication of files</text>
  <text>When activated, this purge script will remove any nmon file older than 1 day:</text>


    <input field="enabled">
      <label>Enable</label>
      <type>bool</type>
    </input>

  <text>Default execution interval to each hour:</text>

    <input field="interval">
      <label>Interval</label>
      <type>text</type>
    </input>
  </block>
  
</setup>